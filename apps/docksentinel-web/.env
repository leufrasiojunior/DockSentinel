# Base consumida pelo browser.
# Padrão implícito: /api
# Para falar direto com a API sem proxy do Vite:
# VITE_API_URL=http://localhost:3000

# Destino do proxy do Vite no desenvolvimento.
# API_PROXY_TARGET=http://localhost:3000
