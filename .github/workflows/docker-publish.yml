name: Publish Docker (tags only)

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  docker:
    runs-on: ubuntu-latest
    environment: ci-ds
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Necessário para builds multi-arch (amd64/arm64)
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Resolve version
        id: version
        shell: bash
        run: |
          echo "value=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      # Gera tags Docker a partir da tag Git (SemVer)
      - name: Docker meta (server)
        id: meta_server
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/docksentinel
          tags: |
            # v1.2.3        -> 1.2.3, 1.2, 1
            # v1.2.3-beta.1 -> 1.2.3-beta.1 (apenas full)
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}

            # Tag por SHA (debug)
            type=sha,format=short,prefix=sha-

      - name: Add channel tags (latest/alpha/beta)
        id: channel
        shell: bash
        run: |
          REF="${GITHUB_REF_NAME}"
          SERVER_EXTRA=""
          # stable: v1.2.3 -> latest
          if [[ "$REF" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            SERVER_EXTRA="${{ secrets.DOCKERHUB_USERNAME }}/docksentinel:latest"
          fi
          # alpha tag: v1.2.3-alpha.1 -> alpha
          if [[ "$REF" == *"-alpha."* ]]; then
            SERVER_EXTRA="${{ secrets.DOCKERHUB_USERNAME }}/docksentinel:alpha"
          fi
          # beta tag: v1.2.3-beta.1 -> beta
          if [[ "$REF" == *"-beta."* ]]; then
            SERVER_EXTRA="${{ secrets.DOCKERHUB_USERNAME }}/docksentinel:beta"
          fi

          echo "server_extra_tags<<EOF" >> $GITHUB_OUTPUT
          echo "$SERVER_EXTRA" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Build and push server (multi-arch)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            APP_VERSION=${{ steps.version.outputs.value }}
          tags: |
            ${{ steps.meta_server.outputs.tags }}
            ${{ steps.channel.outputs.server_extra_tags }}
          labels: ${{ steps.meta_server.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
